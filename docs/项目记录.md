# AI-QA 项目记录

**当前版本**: `v2.7.1`  
**最后更新**: `2026-04-11`  
**仓库地址**: `https://github.com/TomatoAiEgg/Ai-QA`

## 版本总览

| 更新日期 | 更新版本 | 更新记录 |
| --- | --- | --- |
| 2026-04-11 | v2.7.1 | 修复 RocketMQ broker 容器配置挂载路径和数据目录权限问题，云服务器部署后 broker 可稳定运行 |
| 2026-04-11 | v2.7.0 | 恢复邮箱注册，登录/注册增加验证码，关闭前端手机号登录入口，新增工作台昵称修改，优化知识库创建/编辑弹框 |
| 2026-04-10 | v2.6.0 | 后端模块重构，接入 RocketMQ 异步文档处理，默认文档存储切换为 MinIO，补强安全和缓存一致性 |
| 2026-03-30 | v2.5.4 | 新增聊天模型池自动切换与重试、放开 Nginx 和 Spring 上传大小限制、Docker Compose 新增可选 Portainer 运维面板 |
| 2026-03-28 | v2.5.3 | 新增 Docker 一键部署文件，配置迁移到 `application.yml`，日志目录固定到 `/opt/logs/ai-qa`，支持 GitHub 拉取后直接部署 |
| 2026-03-25 | v2.5.2 | RAG 检索切换到新向量表 `vector_store_1024_v2`，聊天页知识库下拉支持“全部知识库”，补充关键业务日志与云服务器部署说明 |
| 2026-03-25 | v2.5.1 | 补充登录页输入框级格式校验，停用 `schema.sql` 自动建表，导出数据库结构与数据脚本 |
| 2026-03-25 | v2.5.0 | 新增 OpenAPI 文档上传能力，修复 Redis 登录态缓存兼容问题，关闭注册入口并重做登录页 |
| 2026-03-24 | v2.4.0 | 接入 Sa-Token + Redis 登录态与查询缓存，前端持久化登录信息，文档直接关联用户 |
| 2026-03-23 | v2.3.0 | MyBatis-Plus 替换 JDBC，修复历史对话 UUID 映射，优化知识库管理与前端工作台样式 |
| 2026-03-23 | v2.2.0 | 对齐前后端接口，修复 SSE 与流式 Markdown，补齐知识库和文档业务接口 |
| 2026-03 | v2.1.0 | 调整前端交互，RAG 页面改为弹窗，聊天页与知识库管理页解耦 |
| 2026-03 | v2.0.0 | 引入多知识库管理，新增知识库与文档关联能力 |
| 2025 | v1.1.0 | 项目更名为 `AI-QA`，引入 `RerankService`，切换 Embedding 模型 |
| 2025 | v1.0.x | 完成基础 AI 对话、流式响应和文档切片预览能力 |

## 最近更新

### v2.7.1

- 修复 RocketMQ broker 的配置挂载路径，从临时目录改为镜像实际配置目录 `/home/rocketmq/rocketmq-5.3.2/conf/broker.conf`。
- 新增 RocketMQ broker 日志目录挂载 `/opt/logs/rocketmq:/home/rocketmq/logs`。
- 云服务器部署时修正 `/opt/data/ai-qa/rocketmq` 和 `/opt/logs/rocketmq` 权限，避免容器内 `rocketmq` 用户无法写入导致 broker 重启。
- 验证云服务器当前前端、后端、PostgreSQL、Redis、MinIO、RocketMQ namesrv、RocketMQ broker、RocketMQ dashboard 均可正常运行。

### v2.7.0

- 账号体系收口为邮箱登录和邮箱注册，前端不再展示手机号登录入口。
- 注册时根据邮箱判断是否已存在，已注册邮箱会提示用户直接登录。
- 登录和注册都增加服务端验证码校验，验证码存入 Redis 并一次性消费，防止绕过前端刷接口。
- 登录接口只接受邮箱、密码和验证码参数，手机号登录兼容入口已从前端关闭。
- 对话工作台顶部昵称改为可点击按钮，支持弹框输入新昵称并实时更新当前登录态。
- 新增后端昵称更新接口，更新成功后返回最新 session，前端同步刷新本地用户信息。
- 优化知识库创建/编辑弹框，改为云控制台风格配置面板，增加顶部说明、输入提示、标签说明和移动端按钮适配。
- 云服务器维护方式明确为拉取或上传最新代码后执行 `docker compose up -d --build`，由 Compose 统一更新前后端和中间件容器。

### v2.6.0

- 完成后端模块重构，按 `auth`、`chat`、`conversation`、`document`、`knowledgebase`、`openapi` 拆分控制层和服务层。
- 文档异步处理链路正式接入 RocketMQ，文档上传、消费、重试、状态流转解耦。
- 文档处理增加幂等保护，避免 MQ 重投或重复消费导致向量数据脏写。
- 文档预览改为直接预览原始文件，不再把切片内容返回前端。
- 新增文档存储抽象，支持本地存储和 MinIO，两种实现可切换。
- 默认文档存储切换为 MinIO，新上传文件进入对象存储。
- 文档预览对 MinIO 文件改为签名地址跳转，本地文件仍支持服务端流式预览。
- 文档上传链路优化：上传时计算文件哈希，向量化和向量写库都改为批量处理。
- 前端知识库页支持文档状态轮询刷新、失败文档重试和原文件预览。
- 前后端文案统一回中文，避免接口成功但前端提示被覆盖。
- 修复聊天页 `v-html` 渲染带来的 XSS 风险。
- OpenAPI token 改为摘要存储，不再明文落库。
- Redis 序列化收紧，去掉过宽的默认反序列化配置。
- 全局异常改为统一返回安全文案，避免内部错误直接泄漏到前端。
- 登录、注册、退出流程补充登录态缓存一致性兜底，减少“接口失败但登录态已变化”的问题。

### v2.5.4

- 新增 `ai.service.model-router` 配置，聊天模型支持模型池、免费额度耗尽自动切换、瞬时网络错误重试。
- 聊天模型默认改为文本模型池：`qwen-plus-2025-01-25`、`qwen-turbo-2025-02-11`、`qwen-long`。
- 向量模型调用增加重试逻辑，减少 `Connection reset` 对文档上传的影响。
- Nginx 新增 `client_max_body_size 50m`。
- Spring 新增 multipart 上传限制配置，默认 `50MB`。
- Docker Compose 新增可选 `portainer` 服务，支持 `--profile ops` 启动容器可视化面板。

## 当前架构

### 后端模块

- `controller.auth`
- `controller.chat`
- `controller.conversation`
- `controller.document`
- `controller.knowledgebase`
- `controller.openapi`
- `service.auth`
- `service.chat`
- `service.conversation`
- `service.document`
- `service.embedding`
- `service.knowledgebase`
- `service.model`
- `service.openapi`
- `mq.rocketmq.document`

### 文档处理链路

1. 用户上传文档
2. 文件写入对象存储
3. 文档记录入库
4. 发送 RocketMQ 文档处理消息
5. 消费者读取原始文件、切片、批量向量化、批量写入向量库并更新文档状态

## 当前默认组件

- PostgreSQL + pgvector
- Redis
- RocketMQ
- MinIO
- DashScope / Qwen 兼容 OpenAI 接口

## 当前状态

当前主线已具备：

- 普通 AI 对话
- RAG 对话
- 多知识库管理
- 邮箱登录、邮箱注册、登录验证码、注册验证码
- 注册邮箱占用检查
- 工作台昵称修改
- 文档上传、删除、重试、原文件预览
- RocketMQ 异步文档处理
- MinIO 文档存储
- OpenAPI token 与文档上传
- Redis 登录态与缓存
- Docker Compose 一键部署

## 当前推荐维护方式

服务器可以直接在项目目录执行：

```bash
git pull origin main
docker compose up -d --build
```

如果服务器访问 GitHub 不稳定，可以本地压缩项目代码上传到服务器，解压后在项目根目录执行同一条命令。数据库、Redis、MinIO、RocketMQ 的数据目录只要没有删除，会继续复用原数据。

## 仍需关注

- 前端主包仍偏大，Vite 构建会提示 chunk 过大。
- 旧的本地文档数据还未迁移到 MinIO，只兼容读取。
- 自动化测试仍偏少，尤其是文档处理、MQ、认证、验证码和缓存一致性链路。
