# AI-QA 前后端分离启动指南

## 项目结构

```
AI-QA/
├── src/          # 后端代码 (Spring Boot)
├── ui/           # 前端代码 (Vue 3 + Vite)
└── pom.xml       # Maven 配置
```

## 启动步骤

### 1. 启动 PostgreSQL (带 pgvector)

```bash
docker-compose up -d
```

**数据库信息**:
- 端口：15432
- 数据库：susan_ai
- 用户：postgres
- 密码：postgres

### 2. 启动后端 (端口 7000)

```bash
cd D:\workspace\包装项目\Ai-QA
mvn spring-boot:run
```

**环境变量**:
```bash
set QWEN_API_KEY=你的千问 API 密钥
```

### 3. 启动前端 (端口 3000)

```bash
cd D:\workspace\包装项目\Ai-QA\ui
npm install  # 首次运行
npm run dev
```

### 4. 访问应用

浏览器打开：**http://localhost:3000**

---

## 架构说明

### 前端
- 技术栈：Vue 3 + Vite + Element Plus
- 开发端口：3000
- 通过 Vite 代理访问后端 API

### 后端
- 技术栈：Spring Boot 3.4.2 + Spring AI 1.0.0-M5
- 服务端口：7000
- 提供 RESTful API

### API 代理
前端通过 `vite.config.js` 中的 proxy 配置，将 `/api` 和 `/ai` 请求代理到后端 `http://localhost:7000`

---

## 常见问题

### Q1: 前端无法访问后端 API

**检查**:
1. 后端是否正常启动（端口 7000）
2. CORS 配置是否正确（WebMvcConfig.java）
3. 前端 proxy 配置是否正确（vite.config.js）

### Q2: 401 API Key 错误

**解决**:
```bash
set QWEN_API_KEY=你的千问 API 密钥
# 重启后端
```

### Q3: 数据库连接失败

**解决**:
```bash
# 检查 PostgreSQL 容器
docker ps

# 重启容器
docker-compose restart
```

---

## 生产部署

### 前端构建
```bash
cd ui
npm run build
```

构建产物输出到 `dist/` 目录，可部署到 Nginx 或其他静态文件服务器。

### 后端打包
```bash
mvn clean package -DskipTests
```

生成的 JAR 文件在 `target/` 目录。

---

**祝你使用愉快！** 🚀
