spring:
  application:
    name: AI-QA
  servlet:
    multipart:
      max-file-size: ${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:50MB}
      max-request-size: ${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:50MB}
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/Ai-QA}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:123456}
    driver-class-name: org.postgresql.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      pool-name: AiQaHikariPool
      maximum-pool-size: 12
      minimum-idle: 4
      connection-timeout: 3000
      validation-timeout: 1000
      idle-timeout: 600000
      max-lifetime: 1800000
      keepalive-time: 300000
      leak-detection-threshold: 60000
      data-source-properties:
        reWriteBatchedInserts: true
  sql:
    init:
      mode: never
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:123456}
      database: 0
      timeout: 5s
  cache:
    type: redis
    redis:
      time-to-live: 30m
      cache-null-values: false
  ai:
    openai:
      enabled: true
      api-key: ${SPRING_AI_OPENAI_API_KEY:${QWEN_API_KEY:}}
      embedding:
        options:
          model: ${AI_EMBEDDING_DEFAULT_MODEL:text-embedding-v3}
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: ${AI_CHAT_DEFAULT_MODEL:qwen-plus-2025-01-25}
          timeout: 120
      connection-pool:
        max-idle: 10
        keep-alive: 300
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

server:
  port: 7000

logging:
  config: classpath:logback-spring.xml

ai:
  service:
    document:
      splitter:
        default-chunk-size: 512
        min-chunk-size-chars: 128
        min-chunk-length-to-embed: 5
        max-num-chunks: 10000
        keep-separator: true
    rerank:
      enabled: true
      model: qwen3-rerank
      min-score: 0.5
      top-n: 3
    model-router:
      chat:
        default-model: ${AI_CHAT_DEFAULT_MODEL:qwen-plus-2025-01-25}
        fallback-models:
          - ${AI_CHAT_FALLBACK_MODEL_1:qwen-turbo-2025-02-11}
          - ${AI_CHAT_FALLBACK_MODEL_2:qwen-long}
        exhausted-ttl: ${AI_CHAT_EXHAUSTED_TTL:PT1H}
        transient-retries: ${AI_CHAT_TRANSIENT_RETRIES:1}
      embedding:
        default-model: ${AI_EMBEDDING_DEFAULT_MODEL:text-embedding-v3}
        fallback-models: []
        exhausted-ttl: ${AI_EMBEDDING_EXHAUSTED_TTL:PT1H}
        transient-retries: ${AI_EMBEDDING_TRANSIENT_RETRIES:1}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false
  type-handlers-package: cn.net.tomatoegg.ai.handler

sa-token:
  token-name: aiqa-token
  timeout: 604800
  active-timeout: -1
  is-concurrent: true
  is-share: true
  token-style: uuid
  is-read-cookie: true
  is-read-header: true
